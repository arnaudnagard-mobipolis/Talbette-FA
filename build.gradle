
plugins {
    id "com.android.application" version '8.13.1' apply false
    id "org.jetbrains.kotlin.android" version "1.9.22" apply false
}

// Optionnel : tâche clean
tasks.register("clean", Delete) {
    delete(rootProject.buildDir)
}
